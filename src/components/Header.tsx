import { AppBar, Box } from "@mui/material"

export default () => {
    return (
        <AppBar sx={{backgroundColor : 'black', height : '30px'}}>
            <Box margin={'auto'}>
                
            </Box>
        </AppBar>
    )
}